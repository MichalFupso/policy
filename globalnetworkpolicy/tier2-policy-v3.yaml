apiVersion: projectcalico.org/v3
kind: GlobalNetworkPolicy
metadata:
  name: tier2.policy-v3
spec:
  tier: tier2
  ingress:
    - action: Allow
      source: {}
      destination: {}
  egress:
    - action: Allow
      source: {}
      destination: {}
  types:
    - Ingress
    - Egress
